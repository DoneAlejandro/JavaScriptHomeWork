'use strict';


let a = 2; // создание переменной а, которая равна 2
let x = 1 + (a *= 2); // в данном примере запись в скобках представляет собой упрощённую запись 2 * 2. иначе пример можно записать x = 1 + (2 * 2). в результате х = 5